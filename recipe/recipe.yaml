context:
  version: "0.2.2"

package:
  name: tirith
  version: ${{ version }}

source:
  url: https://github.com/sheeki03/tirith/archive/refs/tags/v${{ version }}.tar.gz
  sha256: c4e050cde0fb1739599791a299d09a670e6aa48dbb0dc5f2da6ab41d5d2fc265

build:
  number: 0
  script:
    env:
      CARGO_PROFILE_RELEASE_STRIP: symbols
      CARGO_PROFILE_RELEASE_LTO: fat
    content:
      - if: unix
        then:
          - cargo auditable install --locked --no-track --bins -j ${CPU_COUNT} --root ${{ PREFIX }} --path crates/tirith
        else:
          - cargo auditable install --locked --no-track --bins -j %CPU_COUNT% --root %LIBRARY_PREFIX% --path crates/tirith
      - cargo-bundle-licenses --format yaml --output ./THIRDPARTY.yml

requirements:
  build:
    - ${{ stdlib('c') }}
    - ${{ compiler('c') }}
    - ${{ compiler('rust') }}
    - cargo-bundle-licenses
    - cargo-auditable

tests:
  - script: tirith --help

about:
  homepage: https://github.com/sheeki03/tirith
  summary: Your browser would catch this. Your terminal won'.
  description: |
    Tirith guards the gate — intercepts suspicious URLs, ANSI injection, and pipe-to-shell attacks before they execute.
  license: AGPL-3.0-only
  license_file:
    - LICENSE-AGPL
    - LICENSE-COMMERCIAL
    - THIRDPARTY.yml

extra:
  recipe-maintainers:
    - claudiushaag
